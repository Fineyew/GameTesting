POSTGRES_DB=game
POSTGRES_USER=game
POSTGRES_PASSWORD=
VT_ENVIRONMENT=local
# Keep old JSON saves until they are backed up/imported into PostgreSQL.
VT_PLAYER_STORE=json
VT_DEBUG=false
VT_JWT_SECRET=
HTTP_PORT=80
HTTPS_PORT=443
