[project]
name = "veilbound-tides-backend"
version = "0.1.0"
description = "FastAPI backend scaffold for a data-driven mobile fantasy RPG."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["backend/tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
